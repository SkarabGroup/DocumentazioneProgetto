@startuml PostGitCredentialResponse
skinparam componentStyle uml2.5
skinparam linetype ortho
skinparam nodesep 30
skinparam ranksep 30
skinparam ClassAttributeIconSize 0
hide circles

class PostGitCredentialResponse {
    + isSuccess: boolean
    + errorMessage?: string
    ---
    + PostGitCredentialResponse(isSuccess, errorMessage?)
    + {static} success(): PostGitCredentialResponse
    + {static} failure(message): PostGitCredentialResponse
}
@enduml